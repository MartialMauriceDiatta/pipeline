pipeline:
  agent: any
  stages:
    - stage: "Checkout"
      steps:
       script:
        - echo: "Checkout du code source"
    - stage: "Build"
      steps:
        script:
         - echo: "Compilation du code avec javac"
    - stage: "Test"
      steps:
        script:
         - echo: "Exécution des tests avec java HelloWorld"
    - stage: "Deploy"
      steps:
        script:
         - echo: "Déploiement sur le serveur de production en cours"
